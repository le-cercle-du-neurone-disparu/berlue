import numpy as np
import pandas as pd


def preprocess_features(X: pd.DataFrame) -> np.ndarray:
    # TODO: Implémenter preprocess_features
    raise NotImplementedError("preprocess_features n'est pas encore implémenté.")
